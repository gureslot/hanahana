# canon-06 §3 パッチ（探索レート24段=0057・面別解放=0058 を実装済みへ反映）2026-06-22

> 目的：canon-06-gameplay.md の §3 が今も「仮/未実装」のままで、実装済み・適用済みの 0057/0058 に追いついていない。本パッチで §3 を実装に一致させる。
> 原則：**実装（0057/0058 全文）と検算済み数値に厳密一致**。仮値は「仮→実装済み（暫定・Phase 4 で微調整余地）」に格上げするのみで、別の確定値の上書きはしない。設計意図〔06-15 等〕の引用はそのまま温存。
> 適用先：**repo の canon-06-gameplay.md と KB の両方**（Claude Code で str_replace 適用、または手貼り。適用後は KB へ再アップロードして同期）。
> 出典：0057_tansaku_ladder_rate.sql／0058_tansaku_area_gate.sql／tansaku-ladder-spec-2026-06-21.md。代表値は Python 検算済み。

---

## 差し替え1：§3 見出し（現 L46）

### 旧
```
## 3. 探索・放置【proposal⑧⑬⑭｜実装0018は深度のみ＝要追従】
```

### 新
```
## 3. 探索・放置【proposal⑧⑬⑭｜レート24段=0057・面別解放=0058 実装済み／戦力ゲートは構造のみ】
```

---

## 差し替え2：§3-1 ▼実装ブロック（現 L52–53）

### 旧
```
**▼実装**：(面,深度)24段の単調増加（現行0018の「浅2/中5/深10・面で平ら」を置換）。周回スケール＝各周の登り幅を半減し前周最上段から継続（S＋S/2＋S/4…＝**+2Sに収束**・半減比は仮）。レートは (面,周,深度) から算出（マップ保存不要）。
- **【仮・シミュ2026-06-18】レート目安**：EXP ＝ 浅0.2/中0.5/深1.0（EXP/分・実装の約**1/2.5**・3枚それぞれ満額）＝**Lv最大≒Day20（4-5面相当）**。メダル ＝ 実装(浅2/中5/深10)の約**1/6**（深≈2/分・**≈2,000/日**＝残高フラット点）。24段ラダー化はこの総量を保って分布を均す（要すり合わせ）。
```

### 新
```
**▼実装【0057・collect_tansaku／土台0033】**：depth-only の固定レート（浅2/中5/深10・0.5/1.2/2.5）を、`step =(面area−1)×3 + 深度`（浅=1/中=2/深=3・step 1..24）から算出する24段ラダー＋周回半減へ置換。レートは (面,周,深度)=(area, boss_round, depth) から都度算出（マップ保存不要・周は collect 時の boss_round を採用）。
- **1周目レート**：`R_med(step)=0.33+2.97×(step−1)/23`、`R_exp(step)=0.20+1.80×(step−1)/23`。メダルは**デッキ単位**（profiles へ加算）・EXPは編成**3枚それぞれ満額**（加算後 Lv 再計算／レア上限 N30/R40/SR50/SSR60/SP50）。gain＝`floor(経過分×レート)`。帰還（編成カードの解錠）・返却は 0033 と同一。
- **周回半減**：`S=R(24)−R(1)`、周 r の `rate_r(step)=R(1)+S×[ (2−2^(2−r)) + (step−1)/(23×2^(r−1)) ]`。前周最上段 R(24) から継続し登り幅が毎周半減、**上限 = 2×R(24)−R(1) ＝ メダル6.27/分・EXP3.80/分で頭打ち**（発散しない＝収入の安全性）。r=1 は R(step) に一致。
- **【数値は暫定・Phase 4 シミュで微調整余地】**：代表点 面4深(step12)＝メダル**1.75/分（≈2,520/日・1デッキ24h）**・EXP**1.06/分（≈1,530/日・枚）**＝canon-04 §7 の探索/放置≈2,000/日帯に整合（主力1デッキ面4でほぼ到達・放置分は最下段で上乗せ）。半減比1/2・線形ランプ・各段の最終分布は Phase 4 で確定。
```

---

## 差し替え3：§3-2 ▼実装ブロック（現 L57–62）

### 旧
```
**▼実装**：
- 面別解放 ＝ **cleared_stage に相乗り**（面ボス撃破で前進）。**最下段(1面浅)はゲートなしで常時開放**。area↔面の対応の細部は**調整（仮）**。
  - **【意図】** 「ボス1-2を倒したら探索1-3が解放、ボス1-3を倒したら探索2-1が解放」〔06-15〕＝探索解放をボス進行に相乗りさせる（別管理にしない）。最下段の常時開放は放置の受け皿（§3-3）。
- 戦力ゲート ＝ その探索デッキの**本体戦闘力の合計 ≥ しきい値(面N)**（武気＝装備項は不使用＝**探索は武気フリー**）。
  - **【意図】** 「探索は戦力と属性だったり要求されるんじゃなかったんですか？…弱いデッキでも報酬の良い上の面の探索ができてしまいます」〔06-15〕＝ゲートが無いと下の面の探索意義が消えるため必須。**武気フリー**は『武気はボス戦の弾＝探索には回さない』棲み分け。
- **SPは探索デッキに入れられる**【4A・2026-06-17】。**本体戦力ゲートは残す**（SP本体は高い＝MAX**9,600**/枚なのでゲートを超えた farm は許容＝4A）。
```

### 新
```
**▼実装**：
- **面別解放【0058・start_tansaku／土台0036＝実装済み】**：解放ゲート `gate = cleared_stage×3 + boss_round_role`（0=未/1=中A済/2=中B済・0054）＝1周目の累積撃破ノード数。`step=(area−1)×3+depth` は **`gate ≥ step−1` で解放**、未満は `EXPLORE_LOCKED`。**step1（面1浅）は gate≥0 で常時開放**（放置の受け皿・§3-3）。2周目以降は cleared_stage=8→gate≥24＝**全段開放**。検証：ボス1-2(中B)撃破→role=2→gate=2→step3(面1深)解放／ボス1-3(面ボス)撃破→cleared_stage=1,role=0→gate=3→step4(面2浅)解放。
  - **【意図】** 「ボス1-2を倒したら探索1-3が解放、ボス1-3を倒したら探索2-1が解放」〔06-15〕＝探索解放をボス進行に相乗りさせる（別管理にしない）。最下段の常時開放は放置の受け皿（§3-3）。
- **戦力ゲート【構造のみ確定・未実装】**：その探索デッキの**本体戦闘力の合計 ≥ しきい値(面f)**（武気＝装備項は不使用＝**探索は武気フリー**）。本体＝基礎×(1+(Lv−1)×rL)（`start_boss_battle` と同式・★非依存）。**しきい値は単調増加・値は Phase 4 シミュで確定後に後続migrationで `start_tansaku` へ追加**（0058 注）。
  - **【意図】** 「探索は戦力と属性だったり要求されるんじゃなかったんですか？…弱いデッキでも報酬の良い上の面の探索ができてしまいます」〔06-15〕＝ゲートが無いと下の面の探索意義が消えるため必須。**武気フリー**は『武気はボス戦の弾＝探索には回さない』棲み分け。
- **SPは探索デッキに入れられる**【4A・2026-06-17】。**本体戦力ゲートは残す**（SP本体は高い＝MAX**9,600**/枚なのでゲートを超えた farm は許容＝4A）。
```

---

## 差し替え4：§8 ステータス／改訂（現 L135・L138 を修正＋ L137 直後に新エントリ追加）

### 4-a：L135 を差し替え
旧
```
- **要追従/未実装**：探索レート（0018＝深度のみ・浅2/中5/深10 → 24段一列＋周回半減）のみ。※占有ロック（cards.tansaku_deck_no＝**0033**・共通ガード＝**0034**）／ボス周回フロンティア（boss_round/boss_round_stage＝**0038**）は**実装済み**。
```
新
```
- **要追従/未実装**：探索レート（24段ラダー＋周回半減）＝**0057 実装済み**／面別解放ゲート＝**0058 実装済み**。残る未実装＝**探索の戦力ゲート（構造のみ確定・しきい値は Phase 4 後に後続migration）**。※占有ロック（cards.tansaku_deck_no＝**0033**・共通ガード＝**0034**）／ボス周回フロンティア（boss_round/boss_round_stage＝**0038**）は**実装済み**。
```

### 4-b：L138 を差し替え
旧
```
- **仮（調整対象）**：探索レート具体値・半減比／戦力ゲートしきい値／ミッション報酬値・area↔面の対応。
```
新
```
- **仮（調整対象）**：探索レートの最終分布・半減比1/2・線形/幾何ランプ（0057は線形・暫定）／戦力ゲートしきい値／ミッション報酬値・area↔面の対応。
```

### 4-c：L137 の直後に新エントリを追加（挿入）
```
- **2026-06-22 反映**：探索レート24段ラダー＋周回半減を **0057（collect_tansaku）**、面別解放ゲート（`gate=cleared_stage×3+boss_round_role`・`gate≥step−1`・`EXPLORE_LOCKED`）を **0058（start_tansaku）** で実装済みへ更新（§3 見出し・§3-1・§3-2・§8 ステータス）。実フォーミュラと検算済み代表値（面4深＝メダル1.75/分・EXP1.06/分／上限6.27・3.80）を §3-1 に記載。**戦力ゲートは構造のみ確定＝しきい値は Phase 4 後に後続migration**（0058 注）。数値（レート分布・半減比）は Phase 4 で微調整余地。台帳 `schema_migrations` 0001〜0058 整備済み。出典：0057/0058・tansaku-ladder-spec-2026-06-21。
```

---

## 適用後チェック
- §3 見出しに「0057・0058 実装済み」が入っている。
- §3-1 ▼実装に `R_med/R_exp` の式・周回半減式・上限6.27/3.80・面4深代表値がある。
- §3-2 ▼実装の面別解放が `gate=cleared_stage×3+boss_round_role`・`EXPLORE_LOCKED`、戦力ゲートが「構造のみ・未実装」になっている。
- §8 に探索レート/面別解放＝実装済み、戦力ゲート＝Phase 4 後、の整理がある。
- KB の canon-06 も同内容に更新（再アップロード）。
